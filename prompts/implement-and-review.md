For this task, work in phases:

Task: {{task}}

1. Use `subagent` with agent `scout` if more context is needed.
2. Create or update a plan with `plan_annotate`.
3. Implement the change.
4. Use `subagent` with agent `reviewer` to review the result.
5. Address blockers, then report final validation.
